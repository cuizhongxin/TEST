# Spring Boot 配置
server:
  port: 3000
  servlet:
    context-path: /api

spring:
  application:
    name: miniprogram-auth

# 微信小程序配置
wechat:
  miniprogram:
    app-id: wx9b30f4663bae806e
    app-secret: 7361ddd1b132692745c5549d90ee1a82
    # 微信API地址
    api:
      code2session: https://api.weixin.qq.com/sns/jscode2session

# JWT配置
jwt:
  # JWT密钥（至少32字符/256bits）
  secret: xiaofanxingetest123456789abcdefghijk  # 32+ 字符的安全密钥
  expiration: 604800  # token过期时间（秒），7天

# 日志配置
logging:
  level:
    root: INFO
    com.miniprogram: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"


